import SettingsView from "@/features/settings/settings-view";

export const metadata = {
  title: "Configuración · GradeFlow",
};

export default function SettingsPage() {
  return <SettingsView />;
}
